name: Database

on:
  pull_request:
    paths: ['supabase/**', 'docs/phase-0/*.sql', '.github/workflows/db.yml']
  push:
    branches: ['**']
    paths: ['supabase/**', 'docs/phase-0/*.sql', '.github/workflows/db.yml']

jobs:
  migrations:
    name: apply migrations · RLS coverage
    runs-on: ubuntu-latest

    services:
      postgres:
        # postgis/postgis et non postgres : la 0016 exige l'extension, absente
        # de l'image de base. L'image la pré-installe dans `public` au premier
        # démarrage — une étape plus bas la retire pour que la migration reste
        # la seule vérité.
        image: postgis/postgis:17-3.5
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: vitola
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports: ['5432:5432']

    env:
      PGHOST: localhost
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: vitola

    steps:
      - uses: actions/checkout@v5

      - name: Install psql
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      # L'image postgis crée l'extension dans `public` de la base initiale. Le
      # dépôt l'installe dans `extensions` (0016), comme pg_trgm et unaccent
      # depuis 0001 : sans cette purge, `if not exists` saute la création et
      # `extensions.geography` ne résout pas — et `spatial_ref_sys` traînerait
      # dans `public`, où le contrôle de couverture RLS le prendrait pour une
      # table sans policy.
      - name: Drop the pre-installed postgis
        run: psql -c 'drop extension if exists postgis cascade;'

      # The Phase 0 verification file creates its own Supabase stand-ins
      # (auth.users, auth.uid(), storage.*, the anon/authenticated roles),
      # applies the P1 migration and runs 25 behavioural assertions.
      #
      # `SET LOCAL ROLE` outside a transaction is ignored with a WARNING, not an
      # error: the assertion then runs as superuser, bypasses RLS and passes for
      # the wrong reason. ON_ERROR_STOP does not trip on a warning, so the log is
      # scanned for it. Four assertions were green this way until August 2026.
      - name: Apply schema and run assertions
        working-directory: docs/phase-0
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f 03b-verification.sql 2>&1 | tee "$RUNNER_TEMP/assertions.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/assertions.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle s'exécute en superutilisateur et ne teste rien."
            exit 1
          fi

      # PostgreSQL accorde EXECUTE à PUBLIC sur toute fonction, et Supabase y
      # ajoute anon et authenticated. Les deux sont silencieux : le §8 de 0001
      # croyait exposer quatre fonctions, il y en avait neuf sur le projet réel.
      - name: Apply 0002 — function grants
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0002_function_grants.sql

      # Le test des droits d'appel ne tourne plus ici mais après 0005 : il
      # vérifie désormais aussi `owns_review()` et `comment_min_role()`, qui
      # n'existent qu'à partir de 0003 et 0004.

      # 0003 (carnet) et 0004 (commentaires + modération). Même scrutin du
      # journal que ci-dessus : un `SET LOCAL ROLE` hors transaction rend une
      # assertion RLS verte sans qu'elle teste quoi que ce soit.
      - name: Apply 0003 — carnet
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0003_carnet.sql

      - name: Apply 0004 — commentaires et modération
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0004_commentaires_moderation.sql

      - name: Carnet — portées et moyenne publique
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/03_carnet_rls.sql 2>&1 | tee "$RUNNER_TEMP/carnet.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/carnet.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      - name: Commentaires et modération
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/04_comments_moderation.sql 2>&1 | tee "$RUNNER_TEMP/moderation.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/moderation.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0005 ferme dans `ref` et `mod` ce que 0002 n'avait fermé que dans
      # `public`. Après 0004, donc, puisqu'il vise aussi les fonctions de `mod`.
      - name: Apply 0005 — droits d'appel sur ref et mod
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0005_ref_function_grants.sql

      # 0006 ouvre la seule porte d'écriture vers `mod` — une fonction SECURITY
      # DEFINER dans `public`, accordée à la clé de service et à personne
      # d'autre. Son §3 (pg_cron) se déclare absent sur une base nue : c'est
      # attendu ici, et son auto-contrôle vérifie la planification partout où
      # l'extension existe.
      - name: Apply 0006 — signalement et statistiques
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0006_signalement_et_statistiques.sql

      - name: Signalement DSA
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/05_signalement.sql 2>&1 | tee "$RUNNER_TEMP/signalement.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/signalement.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0007 rend `ref` lisible par la clé de service. Sans elle, l'export RGPD
      # répond 500 à tout membre connecté — constaté en HTTP réel, pas supposé.
      - name: Apply 0007 — lecture de ref par la clé de service
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0007_ref_service_role_grants.sql

      # Le seul endroit d'où une régression de ces droits se voit : une
      # migration corrige puis vérifie, donc son auto-contrôle ne peut pas
      # échouer sur ce qu'elle vient d'accorder. Ce fichier n'accorde rien.
      - name: La clé de service lit tout ce que l'export doit rendre
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/06_service_role_reads.sql

      # 0008 pose la cave. Son geste central — fumer un cigare — écrit deux
      # tables dans la même transaction par une fonction SECURITY **INVOKER** :
      # l'atomicité sans le privilège (ADR 0006, D1).
      - name: Apply 0008 — la cave
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0008_cave.sql

      # Le seul endroit d'où se voit une régression des décisions de l'ADR 0006 :
      # ce fichier n'accorde rien, donc il peut échouer là où l'auto-contrôle de
      # 0008 ne le peut pas. Même scrutin du journal qu'ailleurs — un
      # `SET LOCAL ROLE` hors transaction rend une assertion verte pour rien.
      - name: Cave — stock, grand livre et geste atomique
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/07_cave_rls.sql 2>&1 | tee "$RUNNER_TEMP/cave.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/cave.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0009 répare un garde-fou qui se gardait lui-même : le trigger de
      # `profiles` appelait, en droits d'appelant, une fonction fermée aux
      # clients. Aucun membre ne pouvait modifier son profil depuis P1.
      - name: Apply 0009 — le garde-fou du profil
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0009_profile_guard.sql

      # Le test des droits (02) vérifiait un droit ; celui-ci vérifie le
      # comportement, et c'est ce que l'ancien ne pouvait pas voir.
      - name: Profil — modifiable par son membre, role et reputation toujours fermés
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/08_profile_guard.sql 2>&1 | tee "$RUNNER_TEMP/profil.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/profil.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0010 pose le social. Deux de ses décisions ne se voient qu'à l'exécution :
      # le blocage est une policy RESTRICTIVE — la seule sémantique qui puisse
      # retirer une ligne —, et `show_humidor` ouvre les caves sans ouvrir le
      # grand livre, ce qui demande trois policies restrictives de plus.
      - name: Apply 0010 — le fil social
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0010_social.sql

      # 0011 complète la 0010 : les deux clés de confidentialité qui décident
      # d'un AFFICHAGE et non d'un droit se lisent par la même porte que celle
      # qui décide d'un droit, pour que la clé n'ait qu'un interprète.
      - name: Apply 0011 — les trois clés de confidentialité
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0011_profile_privacy.sql

      # 0012 corrige la 0010, et le bug a été trouvé de la seule façon possible :
      # en bloquant quelqu'un dans un navigateur, puis en cherchant comment le
      # débloquer. La policy cachait le profil dans les deux sens, et le seul
      # écran portant « Débloquer » est ce profil.
      - name: Apply 0012 — un blocage se défait
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0012_unblock_is_reachable.sql

      # 0013 corrige la 0010 par omission, et le bug a été trouvé par le
      # NETTOYAGE d'un parcours : une publication réservée aux abonnés répondait
      # 404 à son adresse, donc son bouton « Supprimer » était inatteignable.
      - name: Apply 0013 — une publication se lit à son adresse
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0013_post_card.sql

      # Le seul endroit d'où se voit une régression de l'ADR 0007. Il n'accorde
      # rien. Même scrutin du journal qu'ailleurs — un `SET LOCAL ROLE` hors
      # transaction rend une assertion verte pour rien.
      - name: Social — abonnement, fil keyset, braise, blocage, cave montrée
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/09_social_rls.sql 2>&1 | tee "$RUNNER_TEMP/social.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/social.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0014 pose les clubs, les événements et la messagerie. Trois décisions ne
      # se voient qu'à l'exécution : une conversation a exactement deux
      # personnes en colonnes ordonnées, on n'écrit qu'à qui l'on suit, et un
      # message est lisible d'un modérateur — c'est ce qui fait qu'il n'est pas
      # chiffré de bout en bout, et le mécanisme de l'art. 16 du DSA en dépend.
      - name: Apply 0014 — clubs, événements, messagerie
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0014_clubs_evenements_messagerie.sql

      # 0015 rend la boîte de réception en un appel : « qui est l'autre », « quel
      # est le dernier message » et « combien n'ai-je pas lu » sont trois
      # questions PAR LIGNE, donc la forme même du N+1 que le critère de sortie
      # de P3 interdit au fil.
      - name: Apply 0015 — la boîte de réception en un appel
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0015_conversation_inbox.sql

      - name: Clubs, événements, messagerie — appartenance, présence, paire canonique
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/10_clubs_events_messages.sql 2>&1 \
            | tee "$RUNNER_TEMP/clubs.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/clubs.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0016 pose les lieux (ADR 0011). Trois décisions ne se voient qu'à
      # l'exécution : un lieu naît pending et son auteur ne peut pas le publier
      # (WITH CHECK), la revendication ne s'écrit pas depuis un client, et la
      # note d'un avis est engendrée — le garde-fou §2 est la forme de la
      # donnée, pas une consigne.
      - name: Apply 0016 — les lieux
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0016_lieux.sql

      - name: Lieux — proposition, publication, revendication, avis, rayon
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/11_venues_rls.sql 2>&1 \
            | tee "$RUNNER_TEMP/lieux.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/lieux.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # Les 200 lieux du registre DGDDI (PROVENANCE §7). Sur la base principale
      # et non sur vitola_seed : ce seed exige la 0016, que la base de seed du
      # référentiel n'applique pas. Ses contrôles d'intégrité vivent dans le
      # fichier ; l'idempotence se vérifie ici, comme pour le référentiel.
      - name: Load the venue seed, twice — it must not duplicate
        run: |
          (cd supabase/seed && psql -v ON_ERROR_STOP=1 -f seed_venues.sql)
          before=$(psql -tAc "select count(*) from public.venues")
          (cd supabase/seed && psql -v ON_ERROR_STOP=1 -f seed_venues.sql > /dev/null)
          after=$(psql -tAc "select count(*) from public.venues")
          if [ "$before" != "$after" ]; then
            echo "Le rejeu du seed des lieux a changé le compte : $before -> $after"; exit 1
          fi

      # 0017 pose le journal (ADR 0012). Deux décisions ne se voient qu'à
      # l'exécution : un article naît gated et draft, et un article public ne
      # pointe aucune fiche — deux triggers tiennent la frontière dans les
      # deux sens.
      - name: Apply 0017 — le journal
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0017_editorial.sql

      - name: Journal — qui écrit, qui lit, et la frontière des audiences
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/12_editorial_rls.sql 2>&1 \
            | tee "$RUNNER_TEMP/journal.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/journal.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0018 pose les portes du modérateur (ADR 0013). Ce qui ne se voit qu'à
      # l'exécution : la garde has_min_role('moderator') dans chaque porte, la
      # décision qui emporte son acte dans la même transaction, et le refus des
      # verbes sans bras.
      - name: Apply 0018 — la modération
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0018_moderation.sql

      - name: Modération — qui relève la file, et ce qu'une décision emporte
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/13_moderation_rls.sql 2>&1 \
            | tee "$RUNNER_TEMP/moderation.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/moderation.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      # 0019 donne à ref.lines le status qui lui manquait (ADR 0009, pièce 1).
      # Ce qui ne se voit qu'à l'exécution : une gamme naît en brouillon,
      # invisible de tous sauf des relecteurs, et se publie par un editor —
      # l'asymétrie du wiki, remise à l'endroit.
      - name: Apply 0019 — le status de ref.lines
        run: psql -v ON_ERROR_STOP=1 -f supabase/migrations/0019_ref_lines_status.sql

      - name: Gammes — brouillon invisible, publication par un relecteur
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f supabase/tests/14_ref_lines_status.sql 2>&1 \
            | tee "$RUNNER_TEMP/gammes.log"
          if grep -q 'SET LOCAL can only be used in transaction blocks' "$RUNNER_TEMP/gammes.log"; then
            echo "::error::Une assertion change de rôle hors transaction : elle ne teste rien."
            exit 1
          fi

      - name: Function grants, sur les trois schémas
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/02_function_grants.sql

      # §0.5: a table without RLS breaks the build. Belt and braces — the
      # migration asserts this too, but a future migration might not.
      - name: RLS coverage
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/00_rls_coverage.sql

      # Le contrôle de dérive des types, sur la base que les migrations viennent
      # de construire — donc sur ce que le dépôt décrit, et pas sur ce qu'un
      # projet contient par hasard. Il a été vérifié en retirant une table des
      # types : il échoue, ce qui est le seul moyen de savoir qu'il mord.
      - name: Types drift — database.types.ts décrit-il encore le schéma ?
        run: |
          corepack enable
          pnpm install --frozen-lockfile --silent
          psql -tA -f tooling/scripts/schema-inventory.sql > "$RUNNER_TEMP/inventory.json"
          pnpm tsx tooling/scripts/check-types-drift.ts "$RUNNER_TEMP/inventory.json"

      # The seed runs on its own database: the verification file above inserts
      # fixtures that would otherwise collide with real seed rows.
      - name: Load the seed referential
        env:
          PGDATABASE: vitola_seed
        run: |
          createdb vitola_seed
          psql -v ON_ERROR_STOP=1 -f supabase/tests/00_supabase_stubs.sql
          psql -v ON_ERROR_STOP=1 -f docs/phase-0/03-schema-p1.sql
          psql -v ON_ERROR_STOP=1 -f supabase/migrations/0002_function_grants.sql
          # 0003 pour public.aroma_taxonomy : depuis la roue des arômes, le seed
          # n'écrit plus seulement dans `ref`. Sans elle, la section 6 échoue sur
          # une table absente.
          psql -v ON_ERROR_STOP=1 -f supabase/migrations/0003_carnet.sql
          cd supabase/seed && psql -v ON_ERROR_STOP=1 -f seed.sql

      - name: Seed integrity
        env:
          PGDATABASE: vitola_seed
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/01_seed_integrity.sql

      # Idempotence is not a nice-to-have: the seed will be re-run on every
      # correction to the CSVs, and it must never duplicate a row.
      - name: Seed is idempotent
        env:
          PGDATABASE: vitola_seed
        run: |
          before=$(psql -tAc "select count(*) from ref.cigars")
          before_aromas=$(psql -tAc "select count(*) from public.aroma_taxonomy")
          (cd supabase/seed && psql -v ON_ERROR_STOP=1 -f seed.sql > /dev/null)
          after=$(psql -tAc "select count(*) from ref.cigars")
          after_aromas=$(psql -tAc "select count(*) from public.aroma_taxonomy")
          if [ "$before" != "$after" ]; then
            echo "Le rejeu du seed a changé le nombre de fiches : $before -> $after"; exit 1
          fi
          if [ "$before_aromas" != "$after_aromas" ]; then
            echo "Le rejeu du seed a changé le nombre d'arômes : $before_aromas -> $after_aromas"; exit 1
          fi
          echo "Rejeu sans duplication : $after fiches, $after_aromas arômes"

      # The standalone file is derived. If a CSV changes and nobody regenerates
      # it, the dashboard path silently loads stale data.
      - name: Standalone seed is in sync with the CSVs
        run: |
          corepack enable
          pnpm install --frozen-lockfile --silent
          pnpm tsx tooling/scripts/build-standalone-seed.ts
          git diff --exit-code -- supabase/seed/seed_standalone.sql \
            || { echo "seed_standalone.sql est périmé : lancer pnpm tsx tooling/scripts/build-standalone-seed.ts"; exit 1; }

      - name: Seed paths must not reference third-party sources
        run: |
          # Scans the DATA only. PROVENANCE.md names those sites on purpose, to
          # record that they were not used — matching it would be backwards.
          if grep -niE 'halfwheel|cigaraficionado|cigardojo|cubancigarwebsite|scrape|crawler' \
               supabase/seed/*.csv supabase/seed/seed.sql; then
            echo "Une source tierce est referencee dans les donnees (§2 du brief, art. L341-1 CPI)."
            exit 1
          fi
          echo "Aucune source tierce referencee dans les donnees."
